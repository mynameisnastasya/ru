const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;
const finishLoader=()=>$('.loader')?.classList.add('done');if(document.readyState==='loading')addEventListener('DOMContentLoaded',()=>setTimeout(finishLoader,180),{once:true});else setTimeout(finishLoader,180);addEventListener('load',finishLoader,{once:true});setTimeout(finishLoader,1400);

const header=$('#header');const scrollHeader=()=>header?.classList.toggle('scrolled',scrollY>40);scrollHeader();addEventListener('scroll',scrollHeader,{passive:true});
if('IntersectionObserver' in window){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12});$$('.reveal').forEach(el=>io.observe(el))}else{$$('.reveal').forEach(el=>el.classList.add('visible'))}

const menuBtn=$('#menuBtn'),mobileNav=$('#mobileNav');function setMenu(open){menuBtn?.setAttribute('aria-expanded',String(open));mobileNav?.classList.toggle('open',open);mobileNav?.setAttribute('aria-hidden',String(!open));if(open)mobileNav?.removeAttribute('inert');else mobileNav?.setAttribute('inert','');document.body.classList.toggle('menu-open',open);menuBtn?.setAttribute('aria-label',open?'Close menu':'Open menu')}menuBtn?.addEventListener('click',()=>setMenu(!mobileNav.classList.contains('open')));$$('a',mobileNav).forEach(a=>a.addEventListener('click',()=>setMenu(false)));addEventListener('keydown',e=>{if(e.key==='Escape')setMenu(false)});

if(!reduce){const parallax=$$('.parallax');let raf=0;const run=()=>{raf=0;const vh=innerHeight;parallax.forEach(el=>{const r=el.getBoundingClientRect(),s=Number(el.dataset.speed||.05);if(r.bottom>0&&r.top<vh){const y=(r.top+ r.height/2-vh/2)*-s;$('img',el)?.style.setProperty('transform',`scale(1.06) translate3d(0,${y}px,0)`)}})};addEventListener('scroll',()=>{if(!raf)raf=requestAnimationFrame(run)},{passive:true});run()}

const cursor=$('.cursor');if(cursor&&matchMedia('(pointer:fine)').matches){let x=0,y=0,tx=0,ty=0;addEventListener('pointermove',e=>{tx=e.clientX;ty=e.clientY});const loop=()=>{x+=(tx-x)*.16;y+=(ty-y)*.16;cursor.style.left=x+'px';cursor.style.top=y+'px';requestAnimationFrame(loop)};loop();$$('.look,.circle-link,.big-cta').forEach(el=>{el.addEventListener('mouseenter',()=>cursor.classList.add('show'));el.addEventListener('mouseleave',()=>cursor.classList.remove('show'))})}

if(!reduce&&matchMedia('(pointer:fine)').matches){$$('.magnetic').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();const x=(e.clientX-r.left-r.width/2)*.16,y=(e.clientY-r.top-r.height/2)*.18;el.style.transform=`translate3d(${x}px,${y}px,0)`});el.addEventListener('pointerleave',()=>el.style.transform='')})}

const state={tone:'Smoke / Linen',toneSub:'Cool brunette',img:'assets/ombre.jpg',ends:'Loose waves',length:'Mid-back',contrast:'Soft blend'};
const previewImage=$('#previewImage'),previewTitle=$('#previewTitle'),previewMeta=$('#previewMeta'),briefText=$('#briefText'),status=$('#status');
function update(){previewTitle.textContent=state.tone;previewMeta.textContent=`${state.ends} · ${state.length} · ${state.contrast}`;briefText.textContent=`${state.tone} · ${state.ends} · ${state.length} · ${state.contrast}`;if(previewImage.getAttribute('src')!==state.img){const im=new Image();im.src=state.img;previewImage.style.opacity='.2';previewImage.style.transform='scale(1.04)';im.onload=()=>{previewImage.src=state.img;previewImage.style.opacity='1';previewImage.style.transform=''}}}
$$('.tones button').forEach(btn=>btn.addEventListener('click',()=>{$$('.tones button').forEach(x=>x.classList.remove('active'));btn.classList.add('active');state.tone=btn.dataset.value;state.img=btn.dataset.img;state.toneSub=btn.dataset.sub;update()}));
$$('.options[data-key]').forEach(group=>$$('button',group).forEach(btn=>btn.addEventListener('click',()=>{$$('button',group).forEach(x=>x.classList.remove('active'));btn.classList.add('active');state[group.dataset.key]=btn.dataset.value;update()})));
$$('.look').forEach(card=>{const use=()=>{const b=$$('.tones button').find(x=>x.dataset.value===card.dataset.tone);b?.click();$('#custom')?.scrollIntoView({behavior:reduce?'auto':'smooth'})};$('button',card)?.addEventListener('click',use)});
function brief(){return `Hi GuruDreads! I’d like to discuss a custom order. Direction: ${state.tone}; loose ends: ${state.ends}; length: ${state.length}; contrast: ${state.contrast}. I’ll attach daylight photos of my roots and a reference image. Could you confirm availability, price and timing?`}
async function copy(t){if(navigator.clipboard&&isSecureContext)return navigator.clipboard.writeText(t);const ta=document.createElement('textarea');ta.value=t;document.body.append(ta);ta.select();document.execCommand('copy');ta.remove()}
$('#copyBrief')?.addEventListener('click',async()=>{try{await copy(brief());status.textContent='Brief copied — ready to send.'}catch{status.textContent=brief()}setTimeout(()=>status.textContent='',3500)});
$('#shareBrief')?.addEventListener('click',async()=>{if(navigator.share){try{await navigator.share({title:'GuruDreads custom brief',text:brief()});status.textContent='Brief shared.';return}catch(e){if(e.name==='AbortError')return}}try{await copy(brief());status.textContent='Brief copied — ready to send.'}catch{status.textContent=brief()}setTimeout(()=>status.textContent='',3500)});
update();

function bindImageFallback(img){if(!img||img.dataset.fallbackBound)return;img.dataset.fallbackBound='1';img.addEventListener('error',()=>{if(img.getAttribute('src')!=='assets/ombre.jpg'){img.removeAttribute('srcset');img.src='assets/ombre.jpg';img.style.opacity='1';img.style.transform=''}})}$$('img').forEach(bindImageFallback);new MutationObserver(rs=>rs.forEach(r=>r.addedNodes.forEach(n=>{if(n.nodeType!==1)return;if(n.matches?.('img'))bindImageFallback(n);$$('img',n).forEach(bindImageFallback)}))).observe(document.body,{childList:true,subtree:true});
